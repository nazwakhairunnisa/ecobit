<?php

// namespace App\Http\Middleware;

// use Closure;
// use Illuminate\Http\Request;

// class CheckFocus
// {
//     public function handle(Request $request, Closure $next)
//     {
//         if (auth()->check() && !auth()->user()->focus_selected) {
//             return redirect()->route('select.focus');
//         }

//         return $next($request);
//     }
// }
